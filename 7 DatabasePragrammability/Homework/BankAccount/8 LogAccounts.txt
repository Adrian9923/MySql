TASK: Create another table - logs(log_id, account_id, old_sum, new_sum). Add a trigger to the accounts table that adds a new entry to the logs table every time the sum on the given account changes.

Example
The following data is inserted into the logs table after increasing by 10 the balance of an account with an id of 1.

log_id	
account_id	
old_sum	
new_sum
1

1

123.12

113.12

2

1

145.43

155.43




CREATE TABLE `logs` (
  log_id INT AUTO_INCREMENT PRIMARY KEY,
  account_id INT,
  old_sum DECIMAL(19, 4),
  new_sum DECIMAL(19, 4)
);

DELIMITER $$

CREATE TRIGGER tr_accounts_update
AFTER UPDATE ON accounts
FOR EACH ROW
BEGIN
  
    INSERT INTO logs (account_id, old_sum, new_sum)
    VALUES (NEW.id, OLD.balance, NEW.balance);
  
END; $$


Pana aici e pun restul e in plus ca exemplu
UPDATE `accounts`
SET `balance` = `balance` + 10
WHERE `id` = 1; 

SELECT * FROM `logs`; &&