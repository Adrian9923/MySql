TASK: Write an SQL query that finds the names of all employees whose last names are exactly 5 characters long.




SELECT `first_name`, `last_name` FROM `employees`
WHERE CHAR_LENGTH(`last_name`) = 5;